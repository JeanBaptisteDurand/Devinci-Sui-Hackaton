{
    "name": "sui-package-visualizer",
    "version": "1.0.0",
    "private": true,
    "description": "Full-stack TypeScript project to analyze and visualize Sui Move package dependencies",
    "scripts": {
      "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter web dev\"",
      "db:push": "pnpm --filter server db:push",
      "build": "pnpm --filter server build && pnpm --filter web build"
    },
    "devDependencies": {
      "@types/node": "^20.10.0",
      "concurrently": "^8.2.2",
      "typescript": "^5.3.3"
    }
  }
  
  